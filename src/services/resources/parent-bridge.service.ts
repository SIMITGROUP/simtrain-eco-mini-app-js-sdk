/**
 * This file was automatically generated by simpleapp generator. Every
 * MODIFICATION OVERRIDE BY GENERATEOR
 * last change 2025-06-18
 * Author: --
 */

import { MiniAppApiListParam } from "../../types/bridge.type";
import { MiniAppBridgeService } from "../bridge.service";
import * as Schema from "../../openapi/backend-api";
import {
  CreateResource,
  PatchResource,
  UpdateResource,
} from "../../types/common";

export class MiniAppParentBridgeService {
  private bridge: MiniAppBridgeService;

  private resourceName = "parent";

  constructor(bridge: MiniAppBridgeService) {
    this.bridge = bridge;
  }

  async list(params?: MiniAppApiListParam | undefined) {
    return this.bridge.callApi<Schema.Parent[]>(this.resourceName, "list", {
      body: params,
    });
  }

  async detail(id: string) {
    return this.bridge.callApi<Schema.Parent>(this.resourceName, "detail", {
      id,
    });
  }

  async autoComplete(query: string, data: any) {
    return this.bridge.callApi<Schema.ParentAutoComplete[]>(
      this.resourceName,
      "autoComplete",
      {
        query,
        body: data,
      }
    );
  }

  async searchWithRelation(data: Schema.SearchParentWithRelations) {
    return this.bridge.callApi<Schema.ParentWithRelation[]>(
      this.resourceName,
      "searchWithRelation",
      {
        body: data,
      }
    );
  }

  async create(data: CreateResource<Schema.Parent>) {
    return this.bridge.callApi<Schema.Parent>(this.resourceName, "create", {
      body: data,
    });
  }

  async update(id: string, data: UpdateResource<Schema.Parent>) {
    return this.bridge.callApi<Schema.Parent>(this.resourceName, "update", {
      id,
      body: data,
    });
  }

  async patch(id: string, data: PatchResource<Schema.Parent>) {
    return this.bridge.callApi<Schema.Parent>(this.resourceName, "patch", {
      id,
      body: data,
    });
  }

  openOnScreenForm(id?: string) {
    this.bridge.openOnScreenResourceForm(this.resourceName, {
      id,
    });
  }
}
